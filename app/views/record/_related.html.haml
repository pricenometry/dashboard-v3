- if @related.presence
  .panel.panel-default
    .panel-heading
      %h4 Related
    .panel-body
      %ul.list-unstyled
        - @related.each_with_index do |result, index|
          = render 'search/horizontal_result', result: result
  = render 'layouts/responsive_ad'
